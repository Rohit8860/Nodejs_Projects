<!-- 
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EDIT PAGE</title>
    <%- include('style') -%>
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <table>
        <tr>
            <th>Event Name</th>
            <th>Event Id</th>
            <th>Url</th>
            <th>No of Tickets</th>
            <th>Price</th>
            <th>logins</th>
        </tr>
        <tr>
            <td>
                <%= blog.event_name %>
            </td>
            <td><%= blog.event_id %></td>
            <td><%= blog.event_url %></td>
            <td><%= blog.no_of_tickets %></td>
            <td><%= blog.price %></td>
            <td><%= blog.login %>.slice(0,30)</td>
            <td>
                <button class="btn btn-outline-danger" type="button" id="deleteButton" data-id="${item?._id}">Delete</button>
            - </td> -->
        <!-- </tr>
    </table>
    <a href="/edit/<%= blog.id%>" style="width: 100%;" type="button" class="btn btn-primary btn-lg">EDIT BUTTON 
    </a>
    <a href="http://localhost:3000/" style="width: 100%;" type="button" class="btn btn-secondary btn-lg btn-block">HOME BUTTON</a>
    </body>
</html> --> 
    


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EDIT PAGE</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            padding: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #f2f2f2;
        }
        .btn-container {
            display: flex;
            gap: 10px;
            justify-content: space-between;
        }
    </style>
</head>
<body>
    <table class="table">
        <thead>
            <tr>
                <th>Event Name</th>
                <th>Event Id</th>
                <th>Url</th>
                <th>No of Tickets</th>
                <th>Price</th>
                <th>Logins</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><%= blog.event_name %></td>
                <td><%= blog.event_id %></td>
                <td><%= blog.event_url %></td>
                <td><%= blog.no_of_tickets %></td>
                <td><%= blog.price %></td>
                <td><%= blog.login.slice(0, 30) %></td>
            </tr>
        </tbody>
    </table>

    <div class="btn-container">
        <a href="/edit/<%= blog.id %>" class="btn btn-primary btn-lg" style="width: 45%;">Edit Button</a>
        <a href="http://localhost:3000/" class="btn btn-secondary btn-lg" style="width: 45%;">Home Button</a>
    </div>

    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</body>
</html>
